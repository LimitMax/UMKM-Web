// src/config/features.ts

export const FEATURES = {
  ENABLE_REALTIME: true,
  ENABLE_AI_FEATURES: true,
  ENABLE_ONLINE_ORDERING: true,
  ENABLE_LOCAL_STORAGE_DEMO: true, // Allow fallback if Supabase is down or not configured
};
