'use client';

/**
 * @deprecated Legacy DemoRoleSwitcher component. Demo mode is removed and Supabase Auth is now the primary identity system.
 */
export default function DemoRoleSwitcher() {
  return null;
}
